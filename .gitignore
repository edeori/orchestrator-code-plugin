out/
node_modules/
*.vsix
dist/
.vscode-test/
.DS_Store

# Local, machine-specific MCP mirrors may contain secrets (env vars, tokens) -
# never commit these, only the sanitized template is tracked.
.orchestrator/mcp.local.json
.mcp.json
.codex/config.local.toml
